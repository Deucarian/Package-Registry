# Tweens development-only publication review

This document is generated by AI to explain the contract.

The user approved creating Deucarian/Tweens and publishing its initial develop
branch on 2026-09-11. Main promotion, tags, releases and application adoption are
not part of this approval.

## Review branch only

This branch adds the new package's capability, dependency layer and bounded
0.1.0 lifetime exceptions to the current develop governance snapshot. Tweens CI
pins this reviewed snapshot while the live catalog does not yet list Tweens.
The planned main URL is not evidence of a stable distribution branch.

Do not merge this catalog entry into develop/main until the distribution-policy
remote-ref and clean-consumer gates pass for both channels. Generate and review
matching Installer/Bootstrap fallback catalogs at that time. This snapshot does
not change published catalogs, workflows, validator rules, existing package
dependencies or the architecture baseline.

## Extraction and responsibility

The 2026-09-10 extraction review identified repeated visibility progress and
animated lifecycle handling in UI, Notifications, Report Viewer and Simultria
Activity Visualization. Tweens owns the reusable scheduler, visibility samples,
profiles, explicit animated lifetimes and isolated editor previews. Consumers
retain authoritative visibility, selection, pooling, renderer and physics
policy. Common retains easing and Unity object cleanup; Editor owns the visual
language and controls. No external tween engine is introduced.

The adoption changes remain in their separate review workspace. In particular,
the current UI capability description and all existing dependency edges remain
unchanged; this publication does not claim that their runtime migration shipped.

## Bounded lifetime review

- TweenDiagnosticProvider owns only a monotonic provider-ID counter. Scheduler
  disposal unregisters its diagnostic provider; target data is not retained.
- TweenRuntime owns one persistent runner and a quit guard. It disables idle
  ticking and resets both on subsystem registration.
- TweenVisibilityDefaults owns its explicitly configured/cached project profile,
  loaded flag and reduced-motion preference. Session reset clears all three.

The allowances preserve the original extraction review: 1, 2 and 3 mutable
globals respectively, scoped to version 0.1.0. They do not permit per-object
global registries or an increased type-size budget. Lifecycle tests cover
disposal, external destruction, reversal and sessions without domain reload.
